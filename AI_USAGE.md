# AI Usage Log

## Overview
This document logs the usage of AI tools, prompts, and assistance during the development of DesignArena.

## Assistance Log

### Infrastructure & Repetitive CRUD Setup
- **Scope**: Implemented basic Next.js monolith routes (`POST /api/submissions`, `GET /api/submissions/[id]`, `GET /api/attempts`), UI pages (`/`, `/problems/[id]`, `/submissions/[id]`, `/history`), evaluator interface skeleton, deterministic validation, and unit tests.
- **Model**: Antigravity / Kilo Coding Assistant.

## TODO / Human Ownership
- [ ] Implement AI Evaluator domain logic (`AIEvaluator.ts`) and prompt engineering.
- [ ] Define custom grading criteria and threshold logic.
